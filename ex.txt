define 文
    #define 置き換えたい文字列　置き換えた後の文字列
キャスト演算子
    (変換したい型)式;
論理演算子
    & 論理積（AND)
    | 論理和 (OR)
    ^ 排他的論理和 (EOR)
    ~ 1の補数
シフト演算子
    >> 右シフト
    >> 左シフト
複合代入演算子
    &= 論理積(AND)
    |= 論理和(OR)
    ^= 排他的論理和(EOR)
    >>= 右シフト
    <<= 左シフト

条件演算子
    条件 ? 真の時の処理:偽の時の処理